# Module 16: Auth & Infrastructure

> Parent: [SPEC.md](../../SPEC.md) | Status: ✅ Implemented

Cookie auth (AUTH_USERS env). Docker Compose: Next.js + Ollama + cron (Offr sync 30min, PPR daily). SQLite + local filesystem. Tailscale-ready. .env.example with all config.
